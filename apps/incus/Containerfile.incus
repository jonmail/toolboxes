FROM ghcr.io/ublue-os/ubuntu-toolbox@sha256:bd701b283132a64cc2edc7f823fc3300b7de4d61cdb2f0f95216a9083a9a4e3b

RUN apt-get update && \
    apt-get install -y \
        ca-certificates \
        curl \
        systemd && \
    mkdir -p /etc/apt/keyrings/ && \
    curl -fsSL https://pkgs.zabbly.com/key.asc -o /etc/apt/keyrings/zabbly.asc && \
    echo "deb [signed-by=/etc/apt/keyrings/zabbly.asc] https://pkgs.zabbly.com/incus/stable $(. /etc/os-release && echo ${VERSION_CODENAME}) main" > /etc/apt/sources.list.d/zabbly-incus-stable.list && \
    apt-get update && \
    apt-get install -y \
        btrfs-progs \
        incus \
        incus-ui-canonical \
        iproute2 \
        iptables \
        kmod \
        lvm2 \
        virt-viewer && \
    apt-get clean && \
    mkdir -p /var/lib/lxcfs && \
    groupmod -g 250 incus-admin && \
    groupmod -g 251 incus && \
    systemctl enable incus.socket

CMD ["/sbin/init"]
